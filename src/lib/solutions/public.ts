import { getDatabaseUrl } from '@/lib/database-url';
import { getSolutionCategories } from './model';
import { neon } from '@neondatabase/serverless';
import { previewCategories,type PreviewProduct } from '@/lib/catalog-preview';
export type PublishedProduct=PreviewProduct & {category:string;categories:string[]};
export async function publicProducts():Promise<PublishedProduct[]>{const url=getDatabaseUrl();if(!url)return [];try{const sql=neon(url,{fetchOptions:{cache:'no-store'}});const rows=await sql`SELECT id,catalog_key,published_data->>'name' AS name,published_data->>'kind' AS kind,published_data->>'category' AS category,published_data->'categories' AS categories,published_data->>'problem' AS problem,published_data->>'audience' AS audience,published_data->>'website' AS website FROM founder_solutions WHERE published_data IS NOT NULL ORDER BY CASE catalog_key WHEN 'cord' THEN 0 WHEN 'flouvia' THEN 1 ELSE 2 END,published_at DESC NULLS LAST,id`;return rows.map(row=>({...previewCategories.flatMap(category=>category.products).find(product=>product.catalogId===row.catalog_key),catalogId:row.catalog_key?String(row.catalog_key):undefined,name:String(row.name),description:String(row.problem),feature:String(row.audience),website:String(row.website),provider:row.catalog_key==='cord'?'Flouvia':String(row.name),offering:row.kind as 'Software'|'Agencia'|'Servicio',category:String(row.category),categories:getSolutionCategories({category:String(row.category??''),categories:Array.isArray(row.categories)?row.categories as string[]:undefined}),detailUrl:`/soluciones/${row.id}`}));}catch(error){console.error('Public catalogue unavailable',error instanceof Error?error.name:'UnknownError');return [];}}
