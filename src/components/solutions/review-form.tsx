// Removed: editorial decisions live only in the ops backoffice (ops.shwcs.site),
// which has its own session, audit log and `POST /api/review`. Nothing imports this
// file any more and it is safe to delete.
export {};
