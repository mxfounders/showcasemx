import { requireFounder } from '@/lib/solutions/server';
import { NewSolutionButton } from '@/components/solutions/new-solution-button';
export default async function NewSolutionPage(){await requireFounder(true);return <section className="account-page"><h1 className="text-4xl font-semibold tracking-tight">Tu próxima solución empieza aquí.</h1><p className="mb-8 mt-5 leading-relaxed text-stone-500">Prepara tu ficha pregunta por pregunta. Guarda tu borrador y continúa cuando quieras; nada se publica sin revisión.</p><ol className="mb-10 divide-y divide-stone-200 border-y border-stone-200">{[['Tu solución','Nombre, tipo y todas las categorías en las que encaja.'],['Quién y qué hay detrás','El problema, tu público, creadores, redes, capturas y demo.'],['Contratación y contacto','Precios, implementación, integraciones, soporte y contacto privado.'],['Revisar y enviar','Comprueba tu ficha antes de compartirla con el equipo.']].map(([title,description],index)=><li key={title} className="flex gap-4 py-5"><span className="text-sm text-stone-400">0{index+1}</span><div><h2 className="text-sm font-medium">{title}</h2><p className="mt-1 text-sm text-stone-500">{description}</p></div></li>)}</ol><NewSolutionButton /></section>;}
