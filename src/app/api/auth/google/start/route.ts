import { NextRequest,NextResponse } from 'next/server';
import { beginGoogle,googleCookie,googleConfig } from '@/lib/auth/google';
import { securityLimit } from '@/lib/auth/security';
export async function GET(request:NextRequest){const config=googleConfig();if(!config)return NextResponse.json({error:'Google todavía no está configurado.'},{status:503});if(request.nextUrl.origin!==config.origin)return NextResponse.json({error:'Usa el dominio principal.'},{status:403});try{if(!await securityLimit('google-start','global-start',100))return NextResponse.json({error:'Intenta más tarde.'},{status:429});const result=await beginGoogle(request.nextUrl.searchParams.get('next'));const response=NextResponse.redirect(result.url);response.headers.set('Cache-Control','no-store');response.cookies.set(googleCookie,result.cookie,{httpOnly:true,secure:config.origin.startsWith('https:'),sameSite:'lax',path:'/',maxAge:600});return response;}catch{return NextResponse.json({error:'No pudimos iniciar Google.'},{status:503});}}
